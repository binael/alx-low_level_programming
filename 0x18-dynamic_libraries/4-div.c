#include "main.h"

/**
 * div - divides two numbers
 * @a: first number
 * @b: second number
 *
 * Return: Always 0  (success)
 */

int div(int a, int b)
{
	if (b == 0)
	{
		return (0);
	}

	return (a / b);
}
