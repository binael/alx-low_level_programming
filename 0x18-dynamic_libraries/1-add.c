#include "main.h"

/**
 * add - Prints the sign of numbers
 * @a: Operand which can be negative or positive
 * @b: Operand which can be negative or positive
 *
 * Return: n (number)
 */
int add(int a, int b)
{
	return (a + b);
}
